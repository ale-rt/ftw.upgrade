# Please switch to `collective.ftw.upgrade`

This package has been deprecated n favour of [`collective.ftw.upgrade`](https://github.com/collective/collective.ftw.upgrade).

You can still use this package but version 4 will only serve as a transition step to
`collective.ftw.upgrade`.

Since version 4 this package depends on `collective.ftw.upgrade`.
